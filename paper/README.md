# Thesis source

`main.tex` and its bibliography are the current thesis source files. Figures used
by the report are collected in `Figures/`. The official MLBD front-page wordmark
referenced by `main.tex` is included alongside the source.
