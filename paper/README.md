# Thesis source

`main.tex` and its bibliography are the current thesis source files. Figures used
by the report are collected in `Figures/`.

The Imperial College London logo referenced by `main.tex` is intentionally not
included. Add the approved institutional logo as
`Imperial_College_London_new_logo.png` when compiling the document in Overleaf.
